import { Col, Container, Row } from "@sberdevices/plasma-ui";

const Main: React.FC = () => (
    <Container>
        <Row>
            <Col sizeS={1} sizeM={2} sizeL={3} sizeXL={4}>

            </Col>
        </Row>
    </Container>
);

export default Main;